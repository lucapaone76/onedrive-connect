# OneDrive Skill - Environment Variables Template
# Copy this file to .env and fill in your values
# Run: cp .env.example .env
# NEVER commit the .env file to version control!

# Your OneDrive access token (required)
# Get this by running: python auth_helper.py
# Expires after 1 hour - use refresh token to get a new one
ONEDRIVE_ACCESS_TOKEN=your_access_token_here

# Your refresh token (optional but recommended)
# Allows you to get new access tokens without re-authenticating
# Save this from the auth_helper.py output
ONEDRIVE_REFRESH_TOKEN=your_refresh_token_here

# Your Azure AD Application (client) ID (optional)
# Useful if you're automating token refresh
# Get this from Azure Portal > App registrations > Your app > Overview
AZURE_CLIENT_ID=your_client_id_here

# Microsoft Graph API base URL (optional, has a default)
# Only change this if you need to use a different endpoint
# ONEDRIVE_API_BASE_URL=https://graph.microsoft.com/v1.0
